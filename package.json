{
  "scripts": {
    "generate": "<command-to-generate>",
    "dev": "<command-to-run-dev>"
  },
  "dependencies": {
    "dotenv": "^latest-version",
    "axios": "^latest-version",
    "json2csv": "^latest-version"
  }
}